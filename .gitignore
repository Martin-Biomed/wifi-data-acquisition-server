# The ESP-IDF build and compilation container
.devcontainer

# The VSCode Settings for this specific project
.VSCode

# The ESP_IDF auto-generated config file (created by ESP-IDF build)
sdkconfig

# The "build" directory is generated by the VSCode ESP-IDF Extension (causes issues with Git)
build